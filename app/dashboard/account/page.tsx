'use client';
import AccountGrid from '@/app/components/account/AccountGrid';

export default function AccountsPage() {  
  return (
    <div className="min-h-screen bg-gray-50">
      <AccountGrid />
    </div>
  );
}